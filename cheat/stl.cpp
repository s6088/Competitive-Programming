/*bool ignore_case (const Table &table, const Group &group){return table.capacity <  group.people;}
group group = groups[i];vector <table> :: iterator tableToGive = lower_bound(tables.begin(), tables.end(), group, ignore_case);
Bitset <4> foo/ foo.to_ulong() / .to_ullong() [binary to decimal]
advance (it,5); [it+=5]
char sentence []="Rudolph is 12 years old"; sscanf (sentence,"%s %*s %d",str,&i); [input]
char * pch = strtok (str," ,.-"); while (pch != NULL) {//pch; pch = strtok (NULL, " ,.-");} [regx]
ind = -1; while (true) { ind = s.find("is", ind + 1); if (ind == -1) break; //ind; }
#include <ext/pb_ds/assoc_container.hpp> #include <ext/pb_ds/tree_policy.hpp> using namespace __gnu_pbds;
typedef tree<int, null_type, less<int>, rb_tree_tag, tree_order_statistics_node_update> pbds;
.find_by_order(1) .order_of_key(-5)*/